# BATCH-05 → aggregator 交接清单

> 本文件是 BATCH-05（ecm-dd 工具类 2 个 skill + 数据连接器）向本轮 aggregator（BATCH-03）的
> 交接清单。aggregator 完成 6 个共用索引文件更新后请**删除本文件**。

## 本 batch 已 commit 的变更（只动了自己的目录 + 连接器 + 专属文档）

### 新增 skill（2 个）

1. `skills/ecm-dd-data-verify/`
   - `SKILL.md`、`README.md`
   - `references/dimensions-registry.md`（6 大比对维度字段映射）
   - `references/report-template.md`（标准模式 + fallback 模式报告模板）
   - **无 skill 内 scripts/**——直接引用仓库根 `scripts/tushare_connector.py` / `scripts/qcc_connector.py`

2. `skills/ecm-dd-file-review/`
   - `SKILL.md`、`README.md`
   - `references/common-fields.md`（7 大类文件字段抽取清单）
   - `references/consistency-checks.md`（C-01~C-15 共 15 类跨文件一致性核查场景）
   - `references/formal-defects-checklist.md`（签字 / 盖章 / 日期 / 页数 / 扫描质量 7 大类形式瑕疵）
   - `scripts/scan_folder.py`（目录扫描工具，带 `--json` / `--recursive`）
   - `scripts/fallback_read.py`（pdf/docx/xlsx 外部 skill 缺失时的 pypdf/python-docx/openpyxl 兜底）

### 新增数据连接器（原 BATCH-11 工作，本轮一并完成）

- `scripts/tushare_connector.py`——Tushare Pro 薄封装（`TushareClient` + 6 个预置接口 + `--help`/`--smoke` CLI）
- `scripts/qcc_connector.py`——企查查开放平台薄封装（`QccClient` + 7 个预置接口 + `--help`/`--smoke` CLI）
- `scripts/requirements.txt`——统一依赖（requests / pandas / pypdf / python-docx / openpyxl 均 pin 到次版本）

三者均已通过 `--smoke` 自测；未设置环境变量时 `MissingCredentialError` 可被上层 skill 捕获进入 fallback。

### 新增文档（1 份，本 batch 专属）

- `docs/data-connectors.md`——数据连接器使用说明（脚本清单 / 环境变量 / smoke / CLI 示例 / Python API / 异常分层 / 安全约束 / 故障排查）

## aggregator 需要更新的 6 个共用索引文件

按本仓库并行协作约定，以下 6 个文件由 aggregator 统一 resolve：

### 1. `.claude-plugin/plugin.json`

`skills` 数组追加 2 项（末尾）：

```json
    "skills/ecm-dd-data-verify",
    "skills/ecm-dd-file-review",
```

`dependencies.external_skills` 的 `pdf` / `docx` / `xlsx` 三个条目，`required_by` 列表各追加 `"ecm-dd-file-review"`：

```json
  {
    "name": "docx",
    "required_by": [
      ...现有值...,
      "ecm-dd-file-review"
    ]
  }
```

`pdf` / `xlsx` 同理。

### 2. `docs/skill-roadmap.md`

第 3 节 "ecm-dd" 表中，把以下两行状态从 🟡 草稿 改为 ✅ v0.1.0 可用：

- `ecm-dd:dd-data-verify` | — | 数据自动化比对（Tushare / 企查查 API 交叉验证） | ✅ v0.1.0 可用
- `ecm-dd:dd-file-review` | — | 本地文件批量审阅（读取项目文件夹 PDF/Word/Excel） | ✅ v0.1.0 可用

### 3. `docs/project-plan.md`

"当前状态快照"和 batch 清单表格中，BATCH-05 标 ✅；"跨 skill 共享资源"清单新增：

```
- `docs/data-connectors.md`——数据连接器使用说明（Tushare Pro / 企查查）
- `scripts/tushare_connector.py` / `scripts/qcc_connector.py`——API 薄封装，服务 dd-data-verify
```

### 4. `docs/dependencies.md`

"依赖清单"表格中，`docx` / `pdf` / `xlsx` 三行的"被哪些 skill 依赖"列各追加 `ecm-dd-file-review`。

### 5. `README.md`

"当前可用"表格新增两行：

| `ecm-dd:dd-data-verify` | DD 工具类：Tushare / 企查查外部数据交叉比对 | ✅ v0.1.0 |
| `ecm-dd:dd-file-review` | DD 工具类：项目目录文件批量审阅 + 跨文件一致性核查 | ✅ v0.1.0 |

若 README 另有"数据连接器"或"外部数据源"段落可顺便提及 `scripts/tushare_connector.py` / `scripts/qcc_connector.py` / `docs/data-connectors.md`。

### 6. `CHANGELOG.md`

`[Unreleased]` 段末尾新增：

```markdown
### Added — BATCH-05（ecm-dd 工具类 2 个 skill + 数据连接器脚本）
- `skills/ecm-dd-data-verify/`：DD 工具类 skill。调用 Tushare Pro / 企查查开放平台与客户材料做交叉比对；**不套用 DD Memo 五段式契约**，走独立的"数据比对报告"格式（比对维度与数据源 / 比对结果汇总 / 差异分析 / 建议核实事项 / 数据源局限 / 附件 六段式）；凭证缺失时自动降级为"人工核对清单 + 建议核对渠道"的 fallback 模式。内含 `references/dimensions-registry.md`（6 大维度 40+ 字段映射）、`references/report-template.md`（标准 + fallback 两套模板）。
- `skills/ecm-dd-file-review/`：DD 工具类 skill。对已归位目录做批量文件审阅；先调 Anthropic 官方 `pdf` / `docx` / `xlsx` 外部 skill、缺失时走 `scripts/fallback_read.py`（pypdf / python-docx / openpyxl）兜底；输出"文件审阅摘要"（审阅范围 / 按文件审阅 / 跨文件一致性 / 缺失文件 / 无法读取 / 总体观察 六段式），不做合规结论（合规由业务 DD skill 判断）。内含 `references/common-fields.md`（7 大类 30+ 种 ECM 文件字段清单）、`references/consistency-checks.md`（C-01~C-15 共 15 类跨文件一致性核查场景库）、`references/formal-defects-checklist.md`（签字 / 盖章 / 日期 / 页数 / 扫描质量 / 版本 / 真实性初判 7 类形式瑕疵）、`scripts/scan_folder.py`（目录扫描 + JSON 输出）、`scripts/fallback_read.py`（外部 skill 缺失兜底）。
- `scripts/tushare_connector.py`：Tushare Pro 薄封装。`TushareClient` dataclass + `stock_basic` / `stock_company` / `top10_holders` / `top10_floatholders` / `fina_indicator` / `income` / `balancesheet` 7 个预置接口 + 通用 `call(api_name, params, fields)`；凭证走 `TUSHARE_TOKEN`；异常三层（`MissingCredentialError` / `TushareAPIError` / `TushareTransportError`）；`--smoke` / `--help` / 真实查询 CLI。
- `scripts/qcc_connector.py`：企查查开放平台薄封装。`QccClient` dataclass + `company_basic` / `company_detail` / `shareholders` / `employees` / `intellectual_property` / `penalty` / `change_records` 7 个预置接口 + 通用 `call(path, params)`；凭证走 `QCC_APPKEY` + `QCC_SECRET` 并做 MD5 签名；异常三层 + `--smoke` / `--help` CLI。
- `scripts/requirements.txt`：scripts/ 下所有脚本的 Python 依赖统一清单（requests / pandas / pypdf / python-docx / openpyxl 均 pin 到次版本）。
- `docs/data-connectors.md`：数据连接器使用说明（脚本清单 / 环境变量 / smoke 预检 / CLI 示例 / 作为 Python 库使用 / 异常分层表 / 安全约束 / 故障排查表 / 维护）。

### Changed — BATCH-05 相关
- `.claude-plugin/plugin.json`：`skills` 数组新增 2 个 `ecm-dd-*` 路径；`dependencies.external_skills` 的 `docx` / `pdf` / `xlsx` 三个条目 `required_by` 列表追加 `ecm-dd-file-review`。
- `docs/skill-roadmap.md`：2 个 `ecm-dd:*` 工具类状态从 🟡 草稿 改为 ✅ v0.1.0 可用。
- `docs/project-plan.md`：BATCH-05 标记 ✅；"跨 skill 共享资源"清单新增 `docs/data-connectors.md` + connector 脚本路径；BATCH-11 保留占位但功能已提前完成，可标注 ✅。
- `docs/dependencies.md`：`docx` / `pdf` / `xlsx` 三行 `required_by` 列表各追加 `ecm-dd-file-review`。
- `README.md`："当前可用"表追加 `ecm-dd:dd-data-verify` 和 `ecm-dd:dd-file-review` 两行。
```

## 验收 checklist（BATCH-05 自检）

- [x] 两个 skill SKILL.md frontmatter 完整（name / description / version / license / module / user_role / phase / category / depends_on）
- [x] description 触发关键词密集（中文 + 英文 + 同义词 + 非触发边界）
- [x] 两个工具类 skill **不**套用 `shared/templates/dd-skill-template.md` 的五段式（符合模板说明）
- [x] 输出契约明确（dd-data-verify 六段式 / dd-file-review 六段式），不与业务 DD skill 冲突
- [x] connector 脚本带 `--help` + `--smoke`；API key 走环境变量；异常三层；无 API 时 fallback 明确
- [x] `scripts/package-skill.sh skills/ecm-dd-data-verify` 和 `skills/ecm-dd-file-review` 均成功打包
- [x] 跨 skill 依赖通过相对路径引用（`../../shared/...`、`../../scripts/...`、`../../docs/...`）
- [x] 免责声明引用 `../../DISCLAIMER.md`（不复制全文）
- [x] 不放单独 LICENSE

## 本 batch 专属 sanity 检查（aggregator 可直接跑）

```bash
# 脚本预检
python scripts/tushare_connector.py --smoke
python scripts/qcc_connector.py --smoke
python skills/ecm-dd-file-review/scripts/scan_folder.py skills/ecm-dd-approval/

# 打包测试（用 /tmp 避免沙盒 dist/ 权限坑）
./scripts/package-skill.sh skills/ecm-dd-data-verify --output /tmp/skill-test
./scripts/package-skill.sh skills/ecm-dd-file-review  --output /tmp/skill-test
```

## 清理说明

aggregator 完成上述 6 个共用文件的合并后：

1. 删除本文件 `.batch-05-aggregator-handoff.md`
2. 如果 `dist/ecm-dd-data-verify.skill` / `dist/ecm-dd-file-review.skill` 在仓库沙盒里
   变成了 0 字节占位（子 sandbox 权限坑导致 `rm -f` 失败），用
   `mcp__cowork__allow_cowork_file_delete` 请删除权限后清掉；`dist/` 已在 `.gitignore`
   里，不影响 commit，但干净一点为好
