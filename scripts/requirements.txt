# scripts/ 目录下所有独立脚本的 Python 依赖统一清单
# 安装：pip install -r scripts/requirements.txt
#
# 版本策略：pin 到次版本，兼容 patch 升级（例如 2.31.x）。如需锁版本请在项目环境中自行
# 升级到 requirements.lock。

# 通用网络请求
requests>=2.31,<3.0

# Tushare Pro 返回的 items/fields 转 DataFrame；多个 connector 亦用到
pandas>=2.0,<3.0

# dd-file-review 的本地文件读取
pypdf>=4.0,<6.0
python-docx>=1.1,<2.0
openpyxl>=3.1,<4.0
