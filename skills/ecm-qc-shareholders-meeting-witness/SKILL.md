---
name: ecm-qc-shareholders-meeting-witness
description: 股东会见证意见（见证法律意见书）的内核审查 Skill。当用户上传一份股东会法律见证意见书/见证意见/股东大会法律意见书，要求内核、内核审查、内核复核、质检、审阅、出修订意见、出批注意见、挑错、review、check，或提到"内核意见""见证意见内核""股东会见证内核"等时触发。Skill 按四级工作流对见证意见进行全面审查：(1) 自动从巨潮资讯网（通过 AKShare / cninfo 公开 API，无需密钥）拉取该公司对应届次的"股东会通知"和"董事会决议公告"两份参考文件，拉不到则提示用户上传，都没有则跳过交叉比对；(2) 在获得参考文件的情况下，对见证意见与通知/决议做逐字段事实性交叉核对（日期/时间/届次/地点/股权登记日/议案清单/特别决议标注/关联回避/中小投资者单独计票/董事会届次等 12 项核心字段）；(3) **三种特殊表决方式专项校验**——基于《公司法》第 116 条 / 第 119 条、《上市公司股东会规则》（2025 年修订）第 32 条第 2 款（单独计票）、第 33 条（累积投票）、第 46 条（特殊回购）等，对每条议案打"特别决议 / 累积投票 / 中小投资者单独计票"三个标签，再与通知标注、见证意见表述、表决结果格式四者做强校验（识别 → 核对 → 处理三步法 + 矩阵 X/Y/Z，详见 references/voting-rules.md）；(4) 基于公开规则对文件进行形式审查和实质审查。注意 2026 年起所有上市公司已根据证监会 2024-12-27 过渡期安排撤销监事会改设审计委员会，见证意见中残留"监事"字样应订正。最终输出一份带"内核"作者修订痕迹（Track Changes）和批注（Comments）的 Word 文档。批注按律师风格精简（每条 ≤ 3 句），只针对发现的问题——禁止"已核查无问题"类总结性批注。即使用户只是说"帮我看下这份见证意见有没有问题"或"这份股东会法律意见书能过内核吗"，也应触发本 Skill。
version: 0.1.0
license: MIT
module: ecm-qc
user_role: 内核 / QC 团队
phase:
  - 持续督导阶段
category:
  - 文书审核
depends_on:
  external_skills:
    - docx
---

# 股东大会见证意见内核审查 Skill

## 用途概述

本 skill 面向中国 A 股上市公司/新三板挂牌公司股东（大）会法律见证业务，
扮演"内核小组"角色，对项目组提交的《法律意见书》（见证意见）
进行形式 + 实质 + 常见错误三维度审查，产出一份带"内核"
作者修订痕迹和批注的 Word 文档。

本 skill 依据以下公开规则体系：
- 《公司法》（2024 年修订）
- 《上市公司股东会规则》（2025 年修订，原名《股东大会规则》）
- 《律师事务所从事证券法律业务管理办法》（中国证监会 2019 年第 223 号令）
- 《律师事务所从事证券法律业务执业规则》（中华全国律师协会）第 29–38 条、第 34 条
- 《公开发行证券公司信息披露的编报规则第 12 号——公开发行证券的法律意见书
  和律师工作报告》第 9 条、第 19–23 条
- A 股上市公司股东（大）会见证法律意见书行业通用起草指引模板

---

## 配置项

**修订者名称（Reviewer Name）**

所有 tracked change 和 comment 的 `w:author` 属性默认写为 **`内核`**。
如果用户在对话开始时明确指定了不同的名称（例如 `质控`、`合规`、`DC`、
某律所特定的内部审查团队名等），Claude 应采用用户指定的名称，并在整个
工作流中保持一致。未指定时使用默认值 `内核`。

本文档后续所有示例和指令中出现的 `w:author="内核"`、`--author "内核"`
等均应理解为"作者 = 用户指定的修订者名称（默认 `内核`）"。

---

## 免责声明

本 skill 是 A 股股东（大）会法律见证业务的**辅助工具**。产出的修订意见和批注
**不构成法律意见**，不替代签字律师的专业判断。完整免责声明见本仓库顶层
[DISCLAIMER.md](../../DISCLAIMER.md)。使用本 skill 即表示使用者已阅读并接受该声明。

特别说明：
- 本 skill 依赖的监管规则版本以截至 skill 编写时公开施行者为准；规则更新时使用者应自行核实。
- 交叉比对所用的股东大会通知、董事会决议公告等参考文件，通过巨潮资讯网等公开平台获取，
  数据准确性以披露源头为准。

---

## 资深律师执行标准

执行本 skill 时，必须同时遵循 [senior-lawyer-execution-standards.md](../../shared/templates/senior-lawyer-execution-standards.md)。本 skill 的任何输出不得突破四条底线：事实可追溯、法源可核验、风险可分级、建议可落地；无法核验时必须显式标注。

## 本 skill 的实务加固点

- **公告底稿优先**：见证意见必须与会议通知、董事会决议、股权登记日、表决结果公告和网络投票数据交叉核对。
- **见证边界**：只对召集召开程序、出席资格、表决程序和表决结果发表见证意见，不对议案商业合理性背书。
- **表决结果复算**：特别决议同意比例须 ≥ 2/3；累积投票仅列同意票；单独计票分母为中小投资者所持有效表决权；关联回避表决基数应剔除回避股份。
- **特殊表决方式三标签强校验**：对每条议案分别打"是否特别决议 / 是否累积投票 / 是否中小投资者单独计票"三个标签，再与通知标注、见证意见表述、表决结果格式逐项对照（详见 `references/voting-rules.md`）。通知未标但议案性质应当属于的，用【核实】批注让项目组与公司复核，不在正文改判。
- **2026 年起监事会已撤销**：根据证监会 2024-12-27 过渡期安排，自 2026 年 1 月 1 日起所有上市公司均已修订章程改设审计委员会替代监事会。见证意见**不应再出现"监事"或"监事会"字样**——出席人员、回避名单、中小投资者定义中残留"监事"均属沿用旧模板，应订正。
- **总议案 vs 子议案**：议案被拆成子议案逐项表决时，行业通行做法是只列子议案表决结果。**只有通知提案编码表把总议案单独编码并标"作为投票对象的子议案数（N）"时**才提示核查深交所投票系统是否提供总议案独立票数；其余情形不批注。
- **逐字精确比对**：地点 / 议案名称 / 日期 等关键字段须**逐字**比对，多一字、少一字、空格、标点、全/半角差异都要批（如成都纵横"A3 会议室"漏识别、议案"利润分配方案 / 预案"差异等）。
- **三方任一不一致即批注**：见证意见 vs 通知 vs 决议公告，任一方写错都要批；见证意见对、其他公告错的情形写【核实】提示项目组排查（不动见证意见）。
- **模板锚点保护**：写"建议删除""与下文重复"类批注前必须对照 `references/template-anchors.md` 第三节速查表，避免误判网络投票"程序段"vs"数字段"等行业通行的并存结构。
- **视觉 / Logo 校验**：金杜律所文件禁含 "Mallesons" 字样（2024 年分家），跑 `extract_images.py` 提取图片视觉读图。
- **规范用语**："中国台湾**省**"（不是"中国台湾地区"/"台湾"）；意见书法律适用范围应明确**不包括**港澳台；详见 `form-requirements.md` 规范用语清单。
- **模板残留新形态**：同次董事会决议已确定股东会日期时，"具体时间以董事会发出的会议通知为准"为冗余（F7）；判断须对照董事会决议公告——同次会议已定日期 → 必改，多次会议 → 保留。
- **高风险触发器**：通知期限不足、议案临时新增不合规、回避错误、票数无法支持结论、特别决议未达 2/3 却结论"通过"、累积投票出现反对/弃权、单独计票漏列中小投资者结果、强制累积投票未启用（选举独董 ≥ 2 名 / 控股股东持股 ≥ 30%）、Mallesons 字样残留，应列必改。

## 输出格式契约（三条硬性要求）

**这三条来自用户的明确要求，不得偏离。产出文档必须同时满足：**

### 1. 修订者 = "内核"

所有 `<w:ins>` / `<w:del>` 和 `<w:comment>` 的 `w:author` 属性
**必须**统一写为 `内核`（不是默认的 "Claude"、"Anthropic"
或签字律师姓名）。这覆盖 docx skill 默认使用 "Claude" 的行为。

- 插入 `comment.py` 时必须加 `--author "内核"`
- 直接编辑 document.xml 写 `<w:ins>` `<w:del>` 时必须用 `w:author="内核"`

### 2. 修订模式开启 + 最小显示改动原则

打开 Word 修订模式，让律师能直接看到删/增痕迹。
**关键原则**：只标记真正变动的字符，不要"整段删除 + 整段重写"。

正确示例（原句"我爱你" → 新句"我恨你"）：

```xml
<w:r><w:t>我</w:t></w:r>
<w:del w:author="内核" w:date="..."><w:r><w:delText>爱</w:delText></w:r></w:del>
<w:ins w:author="内核" w:date="..."><w:r><w:t>恨</w:t></w:r></w:ins>
<w:r><w:t>你</w:t></w:r>
```

错误示例（不要这样做）：

```xml
<w:del w:author="内核"><w:r><w:delText>我爱你</w:delText></w:r></w:del>
<w:ins w:author="内核"><w:r><w:t>我恨你</w:t></w:r></w:ins>
```

实务中高频应用场景：
- "2024 年" → "2025 年"：只删/增 "4" → "5"，不要整串改
- "第一次临时股东大会" → "第二次临时股东大会"：只删/增 "一" → "二"
- "9:30" → "14:30"：只删/增 "9" → "14"
- 删除一句不适用的模板话（如网络投票描述）：只删除该句，保留其他

### 3. 解释文字只进批注，不进正文

**正文里禁止出现任何解释性/说明性文字**。所有需要说明的内容
（疑问、要求落实底稿、提醒项目组核查、法规依据、建议）
全部通过 `<w:comment>` 批注呈现。

- ✅ 批注里可以写："【必改】本议案为特别决议事项（《公司法》第 116 条），
      请补'本项议案为股东会特别决议事项，已经出席本次股东会的股东及股东
      代理人所持表决权的三分之二以上同意通过'。"
- ❌ 不要直接把这句话用 `<w:ins>` 插进正文中

批注分类前缀（必须使用）：
- **【必改】** 硬性错误（事实错、法规错、模板残留、基础信息错）
- **【核实】** 需与公司/项目组确认再定的问题
- **【建议】** 措辞优化、行文一致性、体例统一
- **【底稿】** 需落实底稿或更新查验计划的事项

### 4. 批注精简且只针对问题（律师风格）

- **每条批注 ≤ 3 句话**：先指问题，再给改法或法规依据；一句能说清的不要
  写两句。
- **禁止"已核查无问题"类总批**：不要在文件开头或结尾加"经核对……整体合规"
  "三类特殊表决方式专项校验小结：……"等综合意见——这种"此地无银三百两"
  的批注没有实际价值，徒增噪声。**只有发现的问题才写批注**；专项校验
  全部通过的，**不留任何痕迹直接进入下一步**。
- **禁止绕弯子的开头**："本批注涉及……"、"基于法规依据……"、"经审查发现……"
  之类的话一律删除，直接说问题。
- **同类问题合并**：若多个议案都遗漏中小投资者口径，**不要逐条批注**，在
  其中一个最具代表性的位置写一条，列明涉及的议案编号即可。

### 5. 批注前避免错位批注（核对模板锚点）

**硬规则**：写"建议删除某段""该段不应在此处""与下文重复"类批注前，
**必须**先对照 `references/template-anchors.md` 第三节速查表，确认该段不属于
行业模板的固定章节内容。

典型陷阱（已固化在 template-anchors.md §四"易误判为重复的典型对"）：

- **网络投票"程序段" vs "数字段"**：召开程序章节项下的"参与网络投票的
  股东行使表决权 + 信息网络有限公司提供数据"是程序性表述；出席人员资格
  章节项下的"参与网络投票的股东共 X 名……"是统计数字。**两者并存是行业
  通行结构，不要批合并或删除。**
- 召开方式 vs 表决方式（都涉及"现场+网络"）：两段不重复
- 网络投票时间 vs 网络投票程序：两段不重复

如属固定章节内容、表述符合该章节的职能，**不要批注**。

---

## 工作流（五步）

### Step 1 — 定位和预读入文件

```bash
ls /mnt/user-data/uploads/
```

找到待审文件（通常是 `.docx`；如是 `.doc` 先用
`scripts/office/soffice.py --headless --convert-to docx` 转换）。

先用 `extract-text` 把全文读一遍（若有已存在的修订痕迹，用
`pandoc --track-changes=all` 查看）：

```bash
extract-text /mnt/user-data/uploads/xxx.docx > /tmp/review-input.md
```

通读全文形成整体理解：这是哪家公司？哪次会议（年度/临时、届次）？
审议了什么议案？有无特别决议/关联交易/累积投票/独立董事选举？

**并在这一步同时提取三个信息用于 Step 1.5**：
- **股票代码**（6 位数字，通常在开头"致 ×××× 公司"处或签字页公司全称附近能找到）
- **板块**（沪市 6 开头 / 深市 0/3 开头 / 北交所 4/8 开头 → `市场=沪深京`；
  新三板挂牌公司 → `市场=三板`）
- **股东大会召开日期**（YYYYMMDD 格式）

如果股票代码从见证意见里识别不出来（比如仅提及公司简称未给代码），用 web_search
以"公司全称 股票代码"检索一下即可；再不行就直接进入 Step 1.5 的 Level 2（提示用户
补充信息或上传参考文件）。

### Step 1.5 — 拉取并解析参考文件（三级降级）

这一步是为了拿到两份事实核对的"底稿"：**股东大会通知**和**发出这次股东
大会通知的董事会决议公告**。它们是交叉比对的依据——见证意见里写的日期/
届次/议案等字段，都要和这两份文件对照。

**为什么这一步很关键**：行业常见错误模式里最高频的错误是"日期/时间/届次与通知
不一致"（见 `common-errors.md` A 类），这类事实错**没有参考文件是审不出来的**，
内核批注的含金量也会大打折扣。

按以下三级降级执行：

#### Level 1 — 自动拉取

运行脚本：

```bash
python /path/to/skill/scripts/fetch_cninfo_announcements.py \
    --stock-code <股票代码> \
    --meeting-date <YYYYMMDD> \
    --market <沪深京|三板> \
    --output-dir /home/claude/refs
```

（脚本第一次运行前确保依赖齐全：`pip install requests pypdf --break-system-packages`）

脚本返回 JSON 到 stdout，关键字段：
- `shareholders_notice`：股东大会通知的元数据 + PDF 路径 + 文本路径（`text_path`）
- `board_resolution`：董事会决议公告的元数据 + PDF 路径 + 文本路径
- `shareholders_notice_candidates` / `board_resolution_candidates`：所有候选
  （当脚本自动挑错时可手动从候选里选）
- `errors`：遇到的问题列表

退出码：
- **0** — 两份都拿到 → 读取两份 .txt 作为后续交叉比对的依据，进入 Step 2
- **1** — 只拿到一份 → 另一份走 Level 2（提示用户上传缺的那份）
- **2** — 一份都没拿到 → 走 Level 2（提示用户上传两份）

**自动挑选的校验**：脚本会按启发式规则自动挑"最像的那份"，但可能挑错
（例如同一天披露多份董事会决议公告时）。拿到结果后对照见证意见里提到的
董事会届次、股东大会届次，**必要时从候选列表里手动确认**——如果自动挑的
不对，从候选里找正确的 `pdf_url` 重新下载（或提示用户）。

#### Level 2 — 提示用户上传

如果 Level 1 因任何原因未能拿到需要的文件（网络失败、公告未披露、股票代码
识别不出、退市等），向用户明确发出上传请求：

> 【内核提示】自动从巨潮资讯网拉取参考文件未成功（原因：……）。为了做事实
> 性交叉比对，请上传以下文件：
> 
> - [公司简称] [届次] **股东大会通知**（PDF 或 docx）
> - 对应的**董事会决议公告**（即决定召开本次股东大会的那次董事会决议公告）
> 
> 上传后我会继续审查。如果暂时拿不到这两份文件，请回复"跳过比对"，
> 我将继续审查但不做交叉核对。

等用户回复：
- 用户上传了文件 → 读取文件内容（PDF 用 pypdf，docx 用 `extract-text`），
  作为后续交叉比对的依据，进入 Step 2
- 用户只上传了其中一份 → 有哪份用哪份，另一份标为缺失
- 用户回复"跳过比对"或类似表述 → 进入 Level 3

#### Level 3 — 跳过交叉比对，显式告知用户

如果用户确认无法提供参考文件，**在最终输出文档的开头插入一条整体性
批注**（位置：文档标题或收件人段首），明确告知：

> 【内核综合意见】本次内核审查**未进行与股东大会通知、董事会决议公告的
> 交叉核对**（因参考文件未能获取/提供）。因此：
> 
> - 基础信息类字段（召开日期/时间/地点/届次/股权登记日/议案清单/
>   董事会届次等）**未经外部文件核对**，请项目组自行对照《股东大会通知》
>   和相应《董事会决议公告》核查；
> - 本次批注仅基于见证意见文本自身的形式审查和实质审查完成。
> 
> 如需完整内核，请补充上述参考文件后重新提交。

然后只做原有的三维度审查（形式 + 实质 + 常见错误库扫描），不写任何
依赖参考文件证据的批注（即 `cross-check-matrix.md` 里"带参考文件证据版"
的话术都不用）。

#### Level 1 成功后的后续使用

读取 `shareholders_notice.text_path` 和 `board_resolution.text_path` 两个
.txt 文件的内容（`extract-text` 或直接读），作为 Step 2 审查时的"事实
依据"。交叉核对的完整矩阵参见 `references/cross-check-matrix.md`。

---

### Step 2 — 三维度审查（同步记录 issues）

建一个 issue 清单，每条记录：
- **位置**（章节/段落/句子）
- **原文摘录**（几个字即可，用于后面定位）
- **问题类型**（形式 / 实质 / 常见错误）
- **处理方式**（tracked change 改正文 / comment 加批注 / 两者并用）
- **修改后正文 or 批注文案**

**参考清单依次过一遍**（顺序：先做交叉比对，再做清单审查）：

0. **交叉比对审查**（仅 Step 1.5 成功获得参考文件时执行）— 阅读
   `references/cross-check-matrix.md`，按 12 项核心字段 + 推导性检查，把见证
   意见、股东会通知、董事会决议公告**逐字精确**对照，标出任何事实性不一致。
   此步**优先级最高**——事实错是行业常见错误模式里的最高频类别。
   - **三方任一不一致都要批注**——见证意见对、其他两份错的情形也要批【核实】，
     不能因为见证意见对就忽略其他公告的错误（参见 cross-check-matrix.md §原则 2）
   - **地点 / 议案名称 / 日期 必须逐字精确比对**——多字、少字、空格、标点
     差异都要批

1. **特殊表决方式专项校验** — 阅读 `references/voting-rules.md`，按"识别 →
   核对 → 处理"三步法处理特别决议 / 累积投票 / 中小投资者单独计票三种表决
   方式。详见原文。**优先级与 Step 0 等同**。

2. **模板锚点核对** — 阅读 `references/template-anchors.md`，对每段确认所在
   章节是否符合行业模板结构。**写"建议删除""重复""合并"类批注前必须
   先对照此文件第三节速查表**——多段并存（如网络投票"程序段" + "数字段"）
   是行业通行结构，不要误判为重复。

3. **视觉 / Logo 校验** — 阅读 `references/visual-check.md`，跑
   `python3 scripts/extract_images.py /path/to/file.docx` 提取内嵌图片，逐张
   视觉读图。重点：**金杜律所出具的意见书禁含 "Mallesons" 字样**（2024 年
   金杜与 Mallesons 全球分家）。

4. **形式要件审查** — 阅读 `references/form-requirements.md`，
   按 14 项清单 + 规范用语清单（"中国台湾省"等）+ 律所抬头视觉校验逐项勾对。

5. **实质审查** — 阅读 `references/substantive-checklist.md`，
   按实质审查清单 8 类审查项逐项过（第五、第六项与表决方式专项校验联动）。

6. **常见错误扫描** — 阅读 `references/common-errors.md`，
   按行业常见错误模式筛查。**重点核对 C/D/E/F 类新增的高危错误编号
   （C6–C10 / D5–D10 / E6–E12 / F7–F10）**。其中 F7"具体时间以董事会通知
   为准"冗余、F8 用语规范、F9 Mallesons 字样、F10 法律适用范围未排除港澳台
   是本轮新增的高频扣分点。

**跨字段一致性检查**（很关键，常见扣分点）：
- 召开日期/时间：见证意见内 vs 股东会通知 vs 决议公告 三者必须一致
- 届次（第几届第几次董事会、第几次临时股东会）：全文统一
- 公司简称：全文首现定义 → 后续使用 → 决议页 → 签字页，必须一致
- 年份：2025 年的会议不能出现"2024 年"字样（高频错误）
- 议案列表：通知里列的议案 vs 意见书第三章列的 vs 决议内容列的，
  逐项对应无遗漏

**编号类核查硬规则**（避免自动编号/手动文本混搭的误判）：

涉及章节编号、议案编号、列表序号的任何疑问，**在写批注前必须先跑脚本核对
渲染后的实际编号**：

```bash
python3 scripts/render_paragraphs.py /path/to/file.docx
# 单独看某段：
python3 scripts/render_paragraphs.py /path/to/file.docx --para 47
```

项目组经常把 Word 自动编号（`<w:numPr>`）和手动文本（`<w:t>` 里写"一、"
"（二）"）混搭——只看文本会误判"前面没有（一），（二）错了"。脚本会
重建每段的渲染编号，覆盖 CJK 全部格式（一二三、（一）（二）（三）、
1. 2. 3. 等）。

- **渲染后连贯** → **不批注**（视觉一致即合规，体例混搭最多【建议】级）
- **渲染后跳号/错位/重复** → 【必改】并指出正确编号
- 详细决策逻辑、典型场景、伪代码见 `references/numbering-detection.md`

### Step 3 — 准备修订工作目录

```bash
cd /home/claude
cp /mnt/user-data/uploads/xxx.docx ./input.docx
python /mnt/skills/public/docx/scripts/office/unpack.py input.docx unpacked/
```

`unpack.py` 会自动美化 XML、合并相邻 run、转换智能引号。
解包后主要编辑 `unpacked/word/document.xml`。

### Step 4 — 写入修订痕迹和批注

**(A) 修订痕迹（tracked changes）——用 Edit 工具直接改 document.xml**

严格遵循"最小显示改动"原则。典型模式：

**替换几个字**（如"2024" → "2025"）：

```xml
<w:r><w:t>截至</w:t></w:r>
<w:del w:id="1" w:author="内核" w:date="2026-04-23T00:00:00Z">
  <w:r><w:delText>2024</w:delText></w:r>
</w:del>
<w:ins w:id="2" w:author="内核" w:date="2026-04-23T00:00:00Z">
  <w:r><w:t>2025</w:t></w:r>
</w:ins>
<w:r><w:t>年</w:t></w:r>
```

**删除一句**（如挂牌公司残留的《股东大会规则》引用）：把该句
所在的 `<w:r>` 替换成 `<w:del>` 包裹的 `<w:delText>`。如果删的
是整段，还要在 `<w:pPr><w:rPr>` 里加 `<w:del/>` 标记段落标记被删，
避免留下空段。

**插入新内容**（极少用——大部分新增建议都应该放批注里）：
仅在缺漏必要模板文字且没有替代表达时才用 `<w:ins>` 直接补进正文。
情况不确定（比如不知道确切票数）时，一律放批注让项目组自行核实。

**关键 XML 细节**：
- 每条修订用唯一的 `w:id`（1, 2, 3... 自增，与 comment 的 id 独立）
- 日期用 `2026-04-23T00:00:00Z` 格式（今天是 2026-04-23）
- `<w:del>` 内部用 `<w:delText>` 不是 `<w:t>`
- 替换整个 `<w:r>` 块，不要把 `<w:ins>` `<w:del>` 塞进 run 内部
- 原 `<w:r>` 若有 `<w:rPr>` 格式设定（加粗、字体、字号），要复制到
  新的 del/ins 的 `<w:r>` 里，保持格式一致

**(B) 批注（comments）——用 comment.py**

对每条批注：

```bash
python /mnt/skills/public/docx/scripts/comment.py unpacked/ <N> \
  "批注文本（XML 实体已转义的）" \
  --author "内核"
```

其中 `<N>` 是批注 ID（0, 1, 2... 自增）。批注文本如含 `&`
要写成 `&amp;`，含 `'` 用 `&#x2019;`，含 `"` 用 `&#x201C;` / `&#x201D;`。

然后在 document.xml 相应位置插入三个标记（commentRangeStart /
commentRangeEnd / commentReference），这三个标记都是 `<w:p>` 的
直接子节点，**不能**套在 `<w:r>` 里。范例：

```xml
<w:commentRangeStart w:id="0"/>
<w:r><w:t>被批注的原文内容</w:t></w:r>
<w:commentRangeEnd w:id="0"/>
<w:r><w:rPr><w:rStyle w:val="CommentReference"/></w:rPr>
  <w:commentReference w:id="0"/>
</w:r>
```

可以在同一段文字上同时有 tracked change + comment：
commentRangeStart → `<w:del>` + `<w:ins>` → commentRangeEnd →
commentReference。

**(C) 批注撰写风格** — 参考 `references/comment-templates.md`，
使用行业通用的内核批注标准话术。核心原则：
- 明确指向问题，给出修改方向或落实要求
- 必要时引用法规依据（《公司法》《股东会规则》《管理办法》具体条文）
- 对项目组的行动项用动词开头："请补充……"、"请核查……"、
  "请落实底稿……"

### Step 5 — 打包、输出、呈递

```bash
python /mnt/skills/public/docx/scripts/office/pack.py \
  unpacked/ /mnt/user-data/outputs/reviewed.docx \
  --original input.docx
```

`pack.py` 会自动校验 + 小修复（如 durableId 越界、`xml:space`
缺失）。如果校验报错是结构性错误，需要按报错定位到 XML 修复后
重新 pack。

输出文件名建议体现"内核后"语义，例如
`[公司简称]_[届次]_见证意见_内核后_[日期].docx`。

最后用 `present_files` 工具呈递给用户：

```python
present_files(filepaths=["/mnt/user-data/outputs/reviewed.docx"])
```

附一段简短的总结性回复：用了几条 tracked change、几条 comment，
发现的主要问题类别是什么（如"3 条日期/届次事实错、2 条特别决议
表述缺失、4 条批注提醒项目组落实底稿"），**不要**把全部修改内容
重复罗列——律师会在 Word 里直接看。

---

## 引用的参考文件

- `references/cross-check-matrix.md` — 交叉比对矩阵（12 项核心字段 + 推导性检查 +
  逐字精确比对 + 三方任一不一致即批注的判定规则）【Step 1.5 拿到参考文件后使用】
- `references/voting-rules.md` — **三种特殊表决方式专项**（特别决议事项清单、
  累积投票适用规则、中小投资者单独计票清单 + 议案识别关键词 + 与通知的强校验
  矩阵 + 批注模板）【Step 2 步骤 1 必读，与 cross-check-matrix.md 联动】
- `references/template-anchors.md` — **见证意见模板章节锚点**（标准三章 +
  结论意见结构 + 每节必备要素 + 易误判为"重复"的典型对）【Step 2 步骤 2
  必读，写"建议删除"类批注前必查】
- `references/visual-check.md` — **视觉 / Logo 校验**（金杜禁含 "Mallesons"
  字样；律所抬头与出具方一致）【Step 2 步骤 3 必读】
- `references/numbering-detection.md` — **Word 自动编号 vs 手动文本编号识别**
  （写编号类批注前必读，避免混搭误判）
- `references/form-requirements.md` — 形式要件 14 项清单 + 规范用语清单
  （"中国台湾省"等）
- `references/substantive-checklist.md` — 内核实质审查 8 类要点详解
- `references/common-errors.md` — 行业常见错误模式汇总的常见错误库
- `references/comment-templates.md` — 内核批注标准话术模板

## 引用的脚本

- `scripts/fetch_cninfo_announcements.py` — 从巨潮资讯网拉取股东大会通知和
  董事会决议公告，下载 PDF 并抽取文本【Step 1.5 使用】
- `scripts/render_paragraphs.py` — 重建 docx 每段的渲染后编号（解析
  `<w:numPr>` + `numbering.xml`），用于核对自动编号 vs 手动文本编号
  【Step 2 写编号类批注前必跑】
- `scripts/extract_images.py` — 提取 docx 内嵌图片（页眉 / Logo / 签字页等
  媒体文件），用于视觉读图核对 "Mallesons" 字样等问题【Step 2 视觉校验前必跑】

遇到 SKILL.md 没覆盖到的具体场景：
- 独立董事选举 / 累积投票 / 中小投资者单独计票 / 特别决议 →
  **`references/voting-rules.md`**
- 特殊股东大会类型 / 网络投票 / 关联回避 → `references/substantive-checklist.md`
- 其他事项参见对应参考文件

---

## 边界与谨慎处理

**本 skill 不做的事情**：
- 不主动替代律师做法律判断——拿不准的事项一律用【核实】类批注
  提示项目组进一步核查
- 不修改项目组已写明的结论性意见（"合法合规"/"真实有效"等），
  除非存在明显的事实冲突；有疑虑的一律批注提示
- 不访问公开信息做事实核查——无法查证的时间/届次/公告编号等
  一律用【核实】批注要求项目组自行核对，而不是猜测改动

**遇到原文与本 skill 规则不匹配的"可能疏漏"**（例如文件里根本没有
声明事项段）：以【必改】批注提示补充，不自行在正文里补写整段声明。
